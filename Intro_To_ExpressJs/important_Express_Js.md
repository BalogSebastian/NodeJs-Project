first route: we could specify the firts http method to route, we will
use the get method, so any type of get method comes in that looks for the route '/'

^/$ must began with a slash and end with a slash